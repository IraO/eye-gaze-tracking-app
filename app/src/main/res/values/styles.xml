<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mainBlue</item>
        <item name="colorPrimaryDark">@color/mainBlue</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ReadBookTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="ReadingGoalsWindow" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/mainBlue</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="colorAccent">@color/textColor</item>
        <item name="android:colorAccent">@color/textColor</item>
        <item name="buttonTint">@color/textColor</item>
    </style>
</resources>
